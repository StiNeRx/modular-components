# This is an example file.
# In your local development, create a .env file and copy these variables into it.

# --- Server Configuration ---
PORT=3000

# --- Admin Authentication ---
# A secure, random string for signing JSON Web Tokens
JWT_SECRET=a-very-strong-and-long-random-secret
# The password for the admin to log in and get a JWT
ADMIN_PASSWORD=your-secure-admin-password-here

# --- Database (For Production) ---
# When you connect to a real database, your connection string goes here
# Example for PostgreSQL: DATABASE_URL="postgres://user:password@host:port/database"
DATABASE_URL=
